# KMeans-C-Spatial
An optimized K-Means implementation that uses a K-D Tree to perform fast spatial point lookups, significantly reducing the complexity of the assignment phase from O(k*n) to O(log n).
